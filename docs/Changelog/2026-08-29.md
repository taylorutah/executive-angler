# 2026-08-29 — Editorial layout pass + river page fixes

## Article pages (Field Notes, Learn)
- Full-bleed hero images; prose stays on the 680px reading measure.
- Pull quotes, tables, stat blocks, blockquotes, asides share the prose column edges exactly (removed the 1152px breakout rail for text-flow elements; user rejected it as "100% width, looks off").
- In-body media (figure/img/video) capped at a 960px media rail, centered.
- Related-content card grids stay at container width.

## FavoriteButton
- Replaced unrecognizable "hook" glyph with a bookmark icon + visible "Save"/"Saved" label. One shared component; applies to articles, rivers, destinations, fly shops, species, guides, lodges.

## River pages
- Top-fly fix: /api/insights/personal-river joined the retired fly_patterns table, so topFlyName was always null ("—"). Unified YourRecordHere + BestWindowCalculator on src/lib/insights/top-fly.ts: most sessions logged, tie-break by fish then recency; works with 1+ records; "—" only when zero resolvable fly records. 7 unit tests.
- Best Window grid: OPTIMAL FLOW / TOP FLY / PRIMARY TARGET now 3-up on desktop, stacked on mobile.
- Overview collapsed by default behind "Read the river overview" <details> toggle; prose remains in server-rendered HTML for SEO.
- Section reshuffle: personal widgets → live conditions → flies + hatch chart → access → regulations → lodges/guides/shops → editorial blurbs → collapsed overview → nearby rivers.

## Field Notes cards
- Overline now reads CATEGORY · N MIN READ (reading time from readingTimeMinutes, muted color).
- Card title tightened one type step, 2-line clamp.
- Hover excerpt: Fraunces italic, larger, left-aligned, green accent rule (was flat centered body text).
- Story grid 3-col at lg, 4-col at xl so the overline never wraps.

## Admin
- Flies admin 400 fixed: /api/admin/entities whitelisted retired canonical_flies (now a read-only view); real table is flies (213 rows). Entity config rewritten to the 28 live columns; every fly field editable; save path verified.

## Destinations
- Filled the 9 empty destinations (WA, MA, MD, NV, MN, WI, NY, CT, VT): full writeups (tagline, description, meta, climate, regulations, license info, months, species) plus licensed hero images in public/images/destinations/, photographer credits, and coordinates.

## Notes
- FAQ structured data audit: site already emits valid FAQPage JSON-LD everywhere FAQs exist (7 articles, species pages, flies/for pages). No changes needed.
- Deployed via vercel CLI (GitHub auto-deploy remains broken).
